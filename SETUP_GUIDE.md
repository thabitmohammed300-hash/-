# 📱 WESAL - منصة محادثة خاصة
# Wesal - Private Chat Platform

تطبيق محادثة فورية خاص لشخصين فقط مع واجهة حديثة وآمنة.

## ✨ المميزات الرئيسية

- 💬 محادثة خاصة بين شخصين فقط
- ⚡ رسائل فورية مع Socket.io
- 🎤 إرسال صور وفيديوهات
- 🔒 حماية الغرفة بكلمة مرور
- 🌙 تصميم داكن حديث (Dark Theme)
- 🌍 دعم متعدد اللغات (العربية، English، Deutsch)

---

## 🚀 البدء السريع

### المتطلبات قبل البدء
- **Node.js** (الإصدار 14 أو أحدث) - [تحميل من هنا](https://nodejs.org)
- **npm** (يأتي مع Node.js)

### خطوات التثبيت والتشغيل

#### 1️⃣ **نسخ المشروع** (إذا لم تكن قد فعلت)
```bash
git clone https://github.com/thabitmohammed300-hash/wesal2.git
cd wesal2
```

#### 2️⃣ **تثبيت المكتبات المطلوبة**
```bash
npm install
```

ستقوم الأوامر التالية بتثبيت:
- ✅ **Express** - خادم الويب
- ✅ **Socket.io** - الاتصال الفوري
- ✅ **CORS** - للطلبات من مصادر مختلفة
- ✅ **Nodemon** - أداة التطوير (اختيارية)

#### 3️⃣ **تشغيل الخادم**

**في وضع الإنتاج:**
```bash
npm start
```

**في وضع التطوير (مع إعادة تحميل تلقائية):**
```bash
npm run dev
```

#### 4️⃣ **فتح المتصفح**
```
http://localhost:3000
```

---

## 📁 هيكل المشروع

```
wesal2/
├── server.js              # خادم Node.js
├── package.json          # المكتبات والإعدادات
├── public/
│   ├── index.html        # الصفحة الرئيسية
│   ├── chat.html         # صفحة الدردشة
│   ├── styles.css        # التنسيقات
│   └── script.js         # كود JavaScript
├── node_modules/         # المكتبات (تُثبت مع npm install)
└── README.md            # هذا الملف
```

---

## ⚙️ التكوين

### تغيير المنفذ (Port)
إذا كان المنفذ 3000 مستخدماً، عدّل في `server.js`:
```javascript
const PORT = process.env.PORT || 5000; // غيّر 3000 إلى 5000
```

أو استخدم متغير البيئة:
```bash
PORT=5000 npm start
```

---

## 🎮 كيفية الاستخدام

### للمستخدم الأول:
1. اذهب إلى `http://localhost:3000`
2. اضغط **"ابدأ المحادثة"**
3. اختر **"إنشاء غرفة جديدة"**
4. أدخل معرف الغرفة وكلمة المرور
5. اضغط **"إنشاء"**
6. شارك معرف الغرفة والكلمة مع الطرف الآخر

### للمستخدم الثاني:
1. اذهب إلى نفس الرابط `http://localhost:3000`
2. اضغط **"ابدأ المحادثة"**
3. اختر **"الانضمام لغرفة"**
4. أدخل معرف الغرفة وكلمة المرور المرسلة من الطرف الأول
5. اضغط **"انضم"**
6. ابدأ المحادثة! 🎉

---

## 🐛 استكشاف الأخطاء

### ❌ الخطأ: "npm: command not found"
**الحل:** تأكد من تثبيت Node.js من [nodejs.org](https://nodejs.org)

### ❌ الخطأ: "Port 3000 already in use"
**الحل:** غيّر المنفذ:
```bash
PORT=5000 npm start
```

### ❌ الخطأ: "Cannot find module"
**الحل:** أعد تثبيت المكتبات:
```bash
rm -rf node_modules package-lock.json
npm install
```

### ❌ الخطأ: الموقع لا يحمل
**الحل:** 
- تأكد من تشغيل الخادم: `npm start`
- تأكد من الرابط: `http://localhost:3000`
- امسح ذاكرة المتصفح: `Ctrl+Shift+Delete`

### ❌ الخطأ: رسالة الخطأ في وحدة التحكم
**الحل:** اقرأ الخطأ بدقة في Terminal وحاول إعادة تشغيل الخادم

---

## 🔧 الأوامر المتاحة

| الأمر | الوصف |
|------|-------|
| `npm start` | تشغيل الخادم (Production) |
| `npm run dev` | تشغيل الخادم مع إعادة تحميل (Development) |
| `npm install` | تثبيت المكتبات |

---

## 📚 التكنولوجيا المستخدمة

- **Backend:** Node.js + Express
- **Real-time:** Socket.io
- **Frontend:** HTML5 + CSS3 + Vanilla JavaScript
- **Styling:** Custom CSS with Glass Morphism

---

## 🔐 الأمان

- ✅ كلمات مرور لحماية الغرف
- ✅ حد أقصى شخصين في الغرفة الواحدة
- ✅ تنظيف الغرف عند قطع الاتصال
- ⚠️ ملاحظة: للإنتاج، أضف تشفير SSL/HTTPS

---

## 🤝 المساهمة

للإبلاغ عن مشاكل أو اقتراح ميزات:
1. انسخ المستودع (Fork)
2. أنشئ فرع جديد (`git checkout -b feature/amazing-feature`)
3. أضف تغييراتك (`git add .`)
4. احفظ التغييرات (`git commit -m 'Add amazing feature'`)
5. ادفع التغييرات (`git push origin feature/amazing-feature`)
6. أنشئ Pull Request

---

## 📞 الدعم والمساعدة

إذا واجهت مشكلة:
1. تحقق من الأخطاء في Terminal
2. تأكد من تثبيت Node.js بشكل صحيح
3. جرب مسح `node_modules` وإعادة التثبيت
4. افتح issue على GitHub

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر تحت ترخيص MIT.

---

## 👨‍💻 المطور

**ثابت محمد** - [GitHub](https://github.com/thabitmohammed300-hash)

---

**استمتع بمحادثتك الخاصة! 🎉**
